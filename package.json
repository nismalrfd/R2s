{
  "name": "r2s-static",
  "version": "1.0.0",
  "description": "Pure static HTML deployment for Vercel",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.js *.css *.png dist/ || true"
  }
}
